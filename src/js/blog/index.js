// blog/index.js
// Example page entry. Add any new page folder and its .js files are auto-discovered.

/** Page-specific init for blog pages. Called automatically on DOMContentLoaded. */
export function init() {
  console.log('[blog] page module initialised');
}
